import {Button} from "./components/button"

function App() {

  return (
    <div>
      <Button variants="primary" text="Hello" size="md" onClick={()=>{}}></Button>
      <Button variants="secondary" text="Hello" size="md" onClick={()=>{}}></Button>
    </div>
  )
}

export default App
